{
  "schema_version": "jac-skill-v1",
  "name": "memory-manager",
  "purpose": "Maintain bounded working memory and compact long-term knowledge records.",
  "activation": {
    "mode": [
      "model-decision",
      "manual"
    ],
    "when": [
      "session start",
      "after a meaningful decision",
      "after completion of a slice"
    ]
  },
  "inputs": [
    "active task state",
    "existing memory summaries",
    "artifact references"
  ],
  "outputs": [
    "working-memory summary",
    "knowledge item record"
  ],
  "rules": [
    "Keep working memory short.",
    "Store decisions, constraints, rejected paths, unresolved risks.",
    "Never silently rewrite requirements."
  ],
  "warnings": [
    "Chronology dumps waste context.",
    "Irrelevant retrieval floods prompts."
  ],
  "blockers": [
    "A proposed memory record changes requirement meaning."
  ],
  "evidence_emitted": [
    "memory_retrieved",
    "memory_recorded"
  ],
  "examples": [
    "Store that a repo has no test harness instead of storing the whole command transcript."
  ],
  "depends_on": [
    "rules/05-memory-policy.md",
    "hooks/context-budgeter/hook.json"
  ],
  "degrades_to": "Working-memory-only mode.",
  "authority_boundary": "May summarize and retrieve relevant records. It may not alter source-of-truth requirements."
}
