{
  "schema_version": "jac-skill-v1",
  "name": "secret-vault-awareness",
  "purpose": "Keep a hard line between secret material and ordinary configuration.",
  "activation": {
    "mode": [
      "model-decision",
      "manual"
    ],
    "when": [
      "auth work",
      "provider credentials",
      "token or secret-adjacent tasks"
    ]
  },
  "inputs": [
    "config fields",
    "storage path",
    "logging path"
  ],
  "outputs": [
    "secret-boundary note",
    "rotation reminder",
    "redaction actions"
  ],
  "rules": [
    "Prefer encrypted stores.",
    "Do not persist secrets in general settings files.",
    "Keep credentials out of prompts when possible."
  ],
  "warnings": [
    "Examples can normalize insecure handling if not reviewed.",
    "Screenshots and traces can leak more than code."
  ],
  "blockers": [
    "Unsafe persistence or exposure path."
  ],
  "evidence_emitted": [
    "pii_detected"
  ],
  "examples": [
    "Warn if a config template includes a live API token field with sample contents."
  ],
  "depends_on": [
    "workflows/secret-handling.md",
    "docs/secret-handling-model.md"
  ],
  "degrades_to": "Manual secret review checklist.",
  "authority_boundary": "May warn and block unsafe secret handling. It may not expose secrets for convenience."
}
