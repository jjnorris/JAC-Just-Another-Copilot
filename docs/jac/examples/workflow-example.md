# Workflow example

Example session using JAC manually:

1. Open `instructions.md`.
2. Run `docs/jac/workflows/intake-and-clarify.md` to classify ambiguity.
3. Fill `docs/jac/templates/correctness-contract.json`.
4. Run `docs/jac/workflows/plan-review.md` and choose `always_proceed` or `request_review`.
5. Implement one bounded slice with `docs/jac/workflows/bounded-implementation.md`.
6. Finish with `docs/jac/workflows/verify-before-claim.md` and `docs/jac/templates/verification-report.json`.
